// Milestone 1 placeholder — full implementation in auth milestone
export default function LoginPage() {
  return (
    <main className="flex min-h-screen items-center justify-center bg-gray-50">
      <h1 className="text-2xl font-bold text-gray-900">FormFlow — Login</h1>
    </main>
  )
}
